import { FunctionComponent, ReactNode } from 'react';

// Styles
import styles from './styles.module.scss';

const ServicePerformanceGain: FunctionComponent = (): ReactNode => {
	return (
		<div>
			<h1>ServicePerformanceGain</h1>
		</div>
	);
};

export default ServicePerformanceGain;
