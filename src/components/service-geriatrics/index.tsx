import { FunctionComponent, ReactNode } from 'react';

// Styles
import styles from './styles.module.scss';

const ServiceGeriatrics: FunctionComponent = (): ReactNode => {
	return (
		<div>
			<h1>ServiceGeriatrics</h1>
		</div>
	);
};

export default ServiceGeriatrics;
