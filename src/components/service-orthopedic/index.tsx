import { FunctionComponent, ReactNode } from 'react';

// Styles
import styles from './styles.module.scss';

const ServiceOrthopedic: FunctionComponent = (): ReactNode => {
	return (
		<div>
			<h1>ServiceOrthopedic</h1>
		</div>
	);
};

export default ServiceOrthopedic;
